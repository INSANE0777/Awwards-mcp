node_modules/
dist/
*.log
capture-build.mjs

# controller scratch (execution ledger, briefs, review packages)
.superpowers/

# reference motion study artifacts (videos stay in showcase/ref-motion only)
/*.mp4

# parser-drift probe run state (report/snapshots stay local; status.json tracked for CI diffs)
.drift/*
!.drift/status.json

# controller scratch (plans, specs, drift-snapshot captures) — local-only
docs/

# demo GIF raw frames + recording artifacts (only the encoded assets/demo.gif is tracked)
assets/demo-src/
.firecrawl/
